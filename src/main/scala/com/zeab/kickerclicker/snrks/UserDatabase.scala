package com.zeab.kickerclicker.snrks

case class UserDatabase(
                         UserId: String,
                         email: String,
                         password: String,
                         cv: Int
                       )
